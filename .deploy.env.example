# Copy to .deploy.env (git-ignored) and fill in. Read by scripts/deploy-site.sh — the server's
# address and key never go in the repo.
DEPLOY_HOST=user@your.server
DEPLOY_KEY=~/.ssh/your_deploy_key
DEPLOY_PATH=/var/www/eelisp.app
